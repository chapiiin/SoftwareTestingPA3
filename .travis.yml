language: python
python:
- "3.8"

#command to install dependencies
install:
  - pip install pytest
  - pip install pytest-cov
  - pip install coveralls

#command to run tests
script:
  - pytest --cov=calculate_retirement/
  - pytest --cov=calculate_bmi/
after_success:
  - coveralls

